<?php
//require 'PHPMailerAutoload.php';
$mail = new PHPMailer;

//$mail->SMTPDebug = 3;                               // Enable verbose debug output

//
//use gmail...
//http://stackoverflow.com/questions/21937586/phpmailer-smtp-error-password-command-failed-when-send-mail-from-my-server
// set gmail to less secure apps.
// worked 2015-11-07_Sat_23.10-PM
//
$mail->IsSMTP(); // telling the class to use SMTP
$mail->Host       = "smtp.gmail.com"; // SMTP server
//$mail->SMTPDebug  = 1;                     // enables SMTP debug information  1 = client messages 2 = cleint and server messages 
$mail->SMTPAuth   = true;                  // enable SMTP authentication
$mail->SMTPSecure = "ssl";                 // sets the prefix to the servier
$mail->Host       = "smtp.gmail.com";      // sets GMAIL as the SMTP server
$mail->Port       = 465;                   // set the SMTP port for the GMAIL server
$mail->SMTPKeepAlive = true;
$mail->Username   = "dgleba@gmail.com";     // GMAIL username
$mail->Password   = "xxx";            // GMAIL password
// ... use gmail.


$mail->From = 'dgleba@stackpole.com';
$mail->FromName = ("Mailer-Automated-PMDS");
$mail->addAddress('dgleba@gmail.com');     // Add a recipient
//$mail->addReplyTo('dgleba@stackpole.com');
//$mail->addCC('dgleba@gmail.com');
//$mail->addBCC('bcc@example.com');

$mail->addAttachment($tmpfilepathxl . '/gleba,owner__' . $datef1 . '.xlsx');         // Add attachments
$mail->isHTML(true);                                  // Set email format to HTML

$mail->Subject = 'CI list email2';
$mail->Body    = 'See attached <b>file!</b>';
$mail->AltBody = 'See attached file!';

if(!$mail->send()) {
    echo 'Message could not be sent.';
    echo 'Mailer Error: ' . $mail->ErrorInfo;
} else {
    echo 'Message has been sent';
}  
